		private int EmployeeId;

		public int GetEmployeeId()
		{
			return EmployeeId;
		}

		public void SetEmployeeId(int Id)
		{
			 EmployeeId=Id;
		}

		main
		{
			Employee emp=new Employee();
			cw(emp.GetEmployeeId());
			emp.SetEmployeeId(100);
		
		}
	
		EmployeeName;
		Properties
		=========
		Used to provide better way to access private fields.

		Improves encapsulation mechanism.
		Consist of public getter and setter methods.

		getter methods
			Return value from variable
		setter method
			assign values to variables

		properties can be applied for static variables as well
		
		Properties can be Read-Write by default
		Properties can Read-Only 
		Poperties can Write-Only
			
		Validations can be done before assigning value to variables
		-------------------------------------------------------------------------------------------------
		Automatic properties are supported.

			public  int Id
    		
    	    get
     	      {
            return id;
        	}
     	   set
      	     {
            id = value;
           }
    	  }
	
		Automatically implemented prorperties
		public string Name{get;set;}

	How to access Properties

	ClassName.PropertyName	//static variable
	
	objectName.PropertyName     //Instance Variable 
	 

numberArray ---------->20 40 50 70

foreach(dataType variableName in CollectionName)
{
}

foreach(int item in numberArray)
foreach(var item in numberArray)

var Keyword
Implicitly typed local variable

int number=100;
var number=100;
var customer=customerObj;

var numberArray={10,20,30};

difference between object and var keyword  

var keyword
========== 
        Implicitly typed local variable 
	It must be initialized before using.
	Store Arrays , Collections,Object,any primitive data type.

	Where Use?
	Inside particular method.
	foreach loop
	
	Where not Use?

	Can't used at class level.
	Can't be used as method parameter
	Can't be used as return type


	ArrayList
			Product
				Id Name Price
	list	===> p1 , p2 , p3 
	list.Add(c1);

				Customer
		customerId, email,contactNumber
		c1 c2 c3

				Order
	
Collections in C#
	
	Group of elements 				====> one Unit 
	
				=====> Arrays 
				=====> Collections 

	Size is fixed which must be initialized before storing any elements
	Similar data types can be stored.
	Manipulations not easy ----> Search , Print sorted order , Insert at specified position , Remove 
				Search or Sorting techniques
	Array elements can be retrieved by index numbers 

	10  30   20   40   50 

	10 - Index 0 
	30 - Index 1
	20 -Index 2 ......

	Collection can store any type of elements.
	Dynamic in nature , no need define size before storing elements into collection classes.
	---------------------------------------------------------------------------------------------------------------------------------------------
	Pre-Defined methods available ----> Search or Sort or Insert at specified position or delete elements and so on
	provide an Organized way to store elements and retrieve elements 

	Namespace 
		System.Collections ;
		System.Collections.Generic;

		Pre-Defined classes  + methods + Interfaces to store and retrieve elements  + manipulations

		Non Generic Collections 										Generic Collections 
		System.Collections ;												System.Collections.Generic;

		ArrayList														List<T>
		Hashtable														Dictionary<T,T>
		Stack															Stack<T>
		Queue															Queue<T>
		SortedList														SortedDictionary<T,T> 
		
		
		ArrayList
			Dynamic array 
			store any type of elements 
			
			Add()  ---Add element at end
			Insert() , Add element at specified index
			Remove() , RemoveAt() ,
			 Clear() , 

			Contains() , Reverse() , Sort() ,ToArray()
			Insert() ---Store an element at specified index number 
			RemoveAt() ---  delete element by index number ---for loop
			
			clear()		---> clears all elements 
			Contains 	----> search for particular element
			Sort()		----> Numerical and alphabetical sorting allowed 

	Hashtable and SortedList
	=====================
	Dictionary Collections
	-------------------------------------

	Store data with Key/Value pairs
					----------------
	We define our own keys to store values.
	Keys can't be duplicated.
	Hashtable prints data in random order , faster retrieval data possible.
							-----------------------
	SortedList stores data in sorted order, retrieval of slow compared to hashtable.
							 ---------------------

	List of Country Names 
	-----------------------------------
	IND 	India
	Aus		Australia
	SL		SriLanka 
	NZ		NewZealand

	EShop
	
	Customer			Product			Suppliers		Admin

	List Of Customers		---->	 Id , Name , Email , ContactNumber
							====> Arrays , Collections. 
	List of Products			-----> ProductId , Name , Cost , AvailableLocation 

	Non Generic Collections 
	--------------------------------------
	Collection to store All Customer Details 

	ArrayList 	---->		List Of Customers
	listObj.Add(customer);  // WORK 
	listObj.Add(product);    // WORK 


	Generics
			Generic Collection , Generic Class , Generic Interface ....Generic Delegates
	
	Generic Collections
	
	It allows user to store specific type of elements.
	Type Safety
	Compile time type checking done.
	----------------------------------------------
	 
	List<T>
		List<Customer> list=new List<Customer>();
		It accept only customer details .
		
		listObj.Add(customer);  // No Error 
		listObj.Add(product);    //ERROR     ---Compile time type checking 

		CustomerTable
		Id      Name 	EmailID    Contact

		5 Rows 

		Collection to hold All Customer records(5 rows ) 
				====> List<Customer>
		Collection to store All Customer ContactNumbers
				====> ArrayList 
	

	Pre-Defined Interfaces
	======================
	a. IList
		List , ArrayList ---
		Add() ,Remove() , Clear() , Contains() , IndexOf(),Insert() ,RemoveAt()
	b. IEnumerable
		foreach() 	----> CollectionName ---Variable to get FinalResult
		IEnumerable GetEnumerator()
					MoveNext() ---move cursor next item
		for(startIndex ; endingIndex ; Incr/Decr)
		{
		}
		foreach (Product p in ProductsList)
		{
			//Read Only 
			//0 to 100    	//No IndexOutOfRangeException
		}

	c. IDictionary
			Key/Value Pairs
	d. IComparable and IComparer

		Both used for comparing Collection based on Objects properties
		CompareTo()   Compare()
		IComparable     ---Compare Objects by single Property
		IComparer	 ---Compare objects by multiple Object Property

					UserId    UserName     Location

	UserObj1		101	    bbb		l1

	UserObj2		100	    aaa		l3

	UserObj3		105      ccc		l2

	UserList	----> UserObj1,UserObj2,UserObj3

	f. ICollection

 DELEGATES  && EVENTS
		It is a reference type.
		Method invocation determined only at run-time.  ====> delegates used to call them
		Used in event handling
		used to bind Components(Button , TextBox,DDL,RB,CHK,Calender) to Methods in GUI applications
		Create own events , and Customize events (Custom Components or Dynamic Component)

		Publisher -Subscriber model.
		Similar to Function pointers(C and C++)
		-----------------
			
	Delegates hold method reference and call methods 

	Delegate are type safe 
	delegate call methods , matching with its signature
	--------------------------------------------------------------------------------------

	delegate signature =method signature

		void Print(Customer customer)	void Delete(Customer customer){}
		{
		}

	Single delegate can be used to call more than one method.
	Calender Control
	================

		Customize Calendar Control ---> By Creating Own Event (Events)
						Block Sat and Sunday

		Method ---Code that blocks certain date ----Call Method Delegate

		EventHandler ---Pre-Defined Delegate ---> Click Event

		User Defined Delagates

	a. Single or Unicast delegate
			invoke single method
	b. MultiCast delegate
			invoke multiple method
	c. Anonymous method delegate -Lambda Expression 

		Anonymous methods
		----------------->
		void printCustomer()
		{
		cw("""");
			//logic  statement or expression or condition
		}	
		Anonymous methods 			====> method without name
		{
		
			//logic  statement or expression or condition
		}	

		( //Logic  )
		
		Method without any name 

		Expression or conditional logics which is used at a particular block or place.
		No need create separate method 
	
	Pre-defined or Generic delegates				
	--------------------------------
		a. Func 
			Call method with return type --string , double ......
			It requires 1 or more parameters

		b. Action
			Call method with void return type 
			It require Zero or more parameters

		c. Predicate
			Check final result after invocation --bool 
			Represents the method that defines a set of criteria and determines whether the specified object meets those criteria.
			
	Steps
	-----
	Delegate declaration

		<Access-Specifier> delegate <Retutn-Type> Delegate_Name(<Parameter>);
		delegate     ----> Keyword		

	Delegate instantiation(creating an object)

		Delegate_Name delegateObjectName=new Delegate_Name(<Method_Name>);
		
	Delegate Invocation 

		delegateObjectName(<Parameters_Value>);
		
	Delegate Can be passed as a parameter to method
	Delegate can used as RETURN TYPE


			

   PrintButton

	Click ---> Event     		EventHandler (Delegate)     
							Handles event execute method 
													
														====> Print() method which saves report to Pdf Format 
	
				Delegates are used bind Events to certain methods when event raised .

	Delegates used to call methods.
	
	Register ===> Job Portal or Tutorials website 

			RegisterButton 
							===> SendEmail()   SendMessage() 	SendWhatsAppMEssage() 
												
	Single delegate we can multiple methods 	====> void SendEmail()  void  SendMessage() 	void SendWhatsAppMEssage() 

	Type-Safe 
	Delegate signature 		match				=====> 		Method Signature 
					
	
	Anonymous methods

		Pre-defined Extension methods	(LINQ)

				====> Expect Expression(condition) as input ====> Give excepcted result



    EVENT 
	=====
		Notifications user about particular action
		Create own event or Customize in built Events 
		Event itself can't be created without Delegate creation.
		---------------------------------------------------------------------------------------------	
		Delegate used as EventHandler ----> 
		Once event fired or raised , Delegate Handles event shows expected output 
		Event does not have any return type 

	STEPS CREATE AND USE EVENT 
		
	1. CREATE AN EVENT WITH DELEGATE
		<Access-Specifier> delegate <Retutn-Type> Delegate_Name(<Parameter>);
		
	<ACCESS-MODIFIER> event <Delegate_Name> <EventName>;

	2. RAISE OR FIRE AN EVENT
		<Event_Name>();
		create a Nofification_Method used by delegate handle event.
	3. 
	Program.cs 
	CREATE AN EVENTHANDLER WHICH SHOWS NOTIFICATION(METHOD) USER

	4. BIND DELEGATE WITH EVENT 
						====> SUBSCRIBER(CLIENT)
	Event_Name+=new DelegateName(Nofification_MethodName);

	this.button1.Click += new System.EventHandler(this.button1_Click);

	 this.buttonPrint.Click += new System.EventHandler(this.buttonPrint_Click);
	
	5. INVOKE EVENT OR DELEGATE OBJECT 


Late binding

	Employee emp=new HR();
	emp.getDetails();

	emp=new Analyst();

		

			SaveEmployee()										PrintEmployeePDF()

					Button_Click Event binded with Save() and Print() methods  ====> Delegate

				SaveButton												PrintButton			=====> Event ---> Click 
			
					RadioButton			----> Checked
					DropDownList			---> SelectedItem